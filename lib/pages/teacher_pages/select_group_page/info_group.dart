import 'package:flutter/material.dart';

class InfoGroup extends StatelessWidget {
  const InfoGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
